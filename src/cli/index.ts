// cli/ — jogar uma run no terminal, sem suco (ver M5 em docs/BUILD_PLAN.md).
//
// Texto puro: mostra mao, pontas, limiar, mods ativos; aceita jogar peca/redraw;
// abre a loja entre blinds; imprime o Trace como texto. O objetivo e sentir as
// DECISOES o quanto antes, antes de qualquer apresentacao/suco (Lei 11).
//
// Implementacao real no M5. Por enquanto, ponto de entrada do script `npm run play`.

function main(): void {
  console.log('play: ainda nao implementado (ver M5 em docs/BUILD_PLAN.md).')
}

main()

export {}
