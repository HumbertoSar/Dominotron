// harness/ — os testes de sanidade T0–T9 (ver docs/SANITY_TESTS.md).
//
// Roda sobre o Trace headless, sem renderizar nada. Prova que um config NAO esta
// quebrado — nunca que e divertido (Lei 12). Imprime o report-card de uma tela.
//
// Implementacao real no M4. Por enquanto, ponto de entrada do script `npm run harness`.

function main(): void {
  console.log('harness: ainda nao implementado (ver M4 em docs/BUILD_PLAN.md).')
}

main()

export {}
